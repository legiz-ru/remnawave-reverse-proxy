---
import { LinkButton, Code, Card } from '@astrojs/starlight/components';
---

<div class="scroll-down-container" id="scroll-btn">
    <button class="scroll-down-button">
        Прокрутите вниз
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-arrow-down">
            <line x1="12" y1="5" x2="12" y2="19"></line>
            <polyline points="19 12 12 19 5 12"></polyline>
        </svg>
    </button>
</div>

<div class="try-it-out-container" id="try-it-out-content">
    <Card title="Установите прямо сейчас" icon="download">
        <p>Чтобы начать выполните следующую команду в вашем терминале:</p>
        <Code
            lang="bash"
            code="bash <(curl -Ls https://raw.githubusercontent.com/eGamesAPI/remnawave-reverse-proxy/refs/heads/main/install_remnawave.sh)"
        />
        <div class="more-info">
            <p>
                Уверены в себе и готовы к установке? Если нет, у нас есть пошаговые инструкции.
            </p>
            <LinkButton href="/ru/installation/requirements" icon="document">
                Посмотреть инструкцию
            </LinkButton>
        </div>
    </Card>
</div>

<style>
    .scroll-down-container {
        position: absolute;
        bottom: 3rem;
        left: 50%;
        transform: translateX(-50%);
        z-index: 10;
        animation: jump 2s ease-in-out infinite;
    }

    .scroll-down-button {
        background: #0E1914;
        border: 1px solid #2C9870;
        color: #AED9C5;
        padding: 0.5rem 1rem;
        border-radius: 9999px;
        font-weight: normal;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        cursor: pointer;
        transition: filter 0.2s;
    }

    .scroll-down-button:hover {
        filter: brightness(1.15);
    }

    @keyframes jump {
        0%, 100% {
            transform: translateX(-50%) translateY(0);
        }
        50% {
            transform: translateX(-50%) translateY(-15px);
        }
    }

    .try-it-out-container {
        padding-top: 4rem;
        max-width: 49rem;
        margin: auto;
        opacity: 0;
        visibility: hidden;
        transform: translateY(30px);
        transition: opacity 0.6s ease-out, transform 0.6s ease-out;
    }

    .try-it-out-container.visible {
        opacity: 1;
        visibility: visible;
        transform: translateY(0);
    }

    .more-info {
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        gap: 1rem;
        margin-top: 2rem;
        padding-top: 1.5rem;
        border-top: 1px solid var(--sl-color-gray-5);
    }

    .more-info p {
        font-size: var(--sl-text-lg);
        margin: 0;
    }

    .sl-card > p {
        text-align: center;
        font-size: var(--sl-text-xl);
        margin-bottom: 1.5rem;
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const scrollBtn = document.getElementById('scroll-btn');
        const content = document.getElementById('try-it-out-content');
        let isContentVisible = false;

        function showContent() {
            if (isContentVisible) return;
            isContentVisible = true;
            if (scrollBtn) {
                scrollBtn.style.display = 'none';
            }
            if (content) {
                content.classList.add('visible');
            }
        }

        if (scrollBtn) {
            scrollBtn.addEventListener('click', () => {
                showContent();
                if (content) {
                    content.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        }

        window.addEventListener('scroll', () => {
            if (window.scrollY > 50) {
                showContent();
            }
        }, { once: true });
    });
</script>